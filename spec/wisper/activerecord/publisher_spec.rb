require 'spec_helper'

describe Wisper::Activerecord::Publisher do
  it 'has a version number' do
    expect(Wisper::Activerecord::Publisher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
